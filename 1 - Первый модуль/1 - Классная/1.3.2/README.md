# cycles-test2

А сколько чисел выведет этот цикл?

```
for (int i = 0; i < 3; ++i) {
	for (int j = 0; j < 3; ++j) {
		if (j == i) {
			continue;
		}

		std::cout << i * j << " ";
	}
}
```

Нужно вывести именно количество, а не сами числа

6
